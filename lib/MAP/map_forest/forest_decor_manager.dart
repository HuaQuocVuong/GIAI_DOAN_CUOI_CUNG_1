import 'package:flame/game.dart';

class ForestDecorManager {
  // Thêm decor cho forest map
  static Future<void> addDecorToGame(FlameGame game) async {      
    // Forest map hiện tại chưa có decor
  }
  // Remove decor
  static Future<void> removeDecorFromGame(FlameGame game) async {
    // Forest map không có decor nên không cần xóa
  }
}